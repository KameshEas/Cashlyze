import { View } from 'react-native';
import HomeView from '../views/HomeView';

export default function AppNavigator() {
  // Placeholder for future navigation (e.g., React Navigation)
  return (
    <View style={{ flex: 1 }}>
      <HomeView />
    </View>
  );
}